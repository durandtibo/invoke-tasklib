# Reusable workflow for testing the *published* PyPI package (as opposed to
# ci-* workflows, which test the repo's source). Installs the package with
# the given command and runs a basic import/smoke check.

name: PyPI Package Install Test
on:
  workflow_call:
    inputs:
      install-cmd:
        default: "uv pip install invoke-tasklib"
        description: "Command to install the package"
        required: false
        type: string
      test-matrix:
        description: "JSON test matrix from lib-get-test-matrix.yaml, loaded once by the caller"
        required: true
        type: string

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 5
    strategy:
      max-parallel: 8
      fail-fast: false
      matrix:
        os: ${{ fromJson(inputs.test-matrix).os_matrix }}
        python-version: ${{ fromJson(inputs.test-matrix).python_versions }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # ratchet:actions/checkout@v7

      - name: Install uv and set the python version
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # ratchet:astral-sh/setup-uv@v10.1.0
        with:
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: Install package
        env:
          INSTALL_CMD: ${{ inputs.install-cmd }}
        run: |
          $INSTALL_CMD

      - name: Verify installed package
        uses: ./.github/actions/verify-installed-package
