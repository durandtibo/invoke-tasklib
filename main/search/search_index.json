{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#overview","title":"Overview","text":"<p><code>invoke-tasklib</code> is a reusable library of Invoke tasks shared across Python projects. Instead of copy-pasting the same <code>tasks.py</code> boilerplate (formatting, linting, type-checking, testing, environment setup, releasing, and documentation) into every repository, a project imports one namespace and gets a consistent set of <code>invoke</code> commands.</p> <p>Quick Links:</p> <ul> <li>Get Started</li> <li>User Guide</li> <li>Config</li> <li>Troubleshooting</li> </ul>"},{"location":"#why-invoke-tasklib","title":"Why invoke-tasklib?","text":"<p>Most Python projects end up with a <code>tasks.py</code> that wraps the same handful of tools: <code>ruff</code>, <code>pyright</code>, <code>pytest</code>, <code>docformatter</code>, <code>uv</code>, <code>mike</code>. Keeping those wrappers in sync across many repositories is tedious, and small inconsistencies (a missing <code>--xdoctest</code> flag, a different coverage flag) creep in over time.</p> <p><code>invoke-tasklib</code> centralizes these tasks in one importable package:</p> <pre><code>from invoke_tasklib import ns\n</code></pre> <pre><code>invoke --list\n</code></pre> <p>A single <code>invoke.yaml</code> at the project root configures the package name and, optionally, the paths used by the tasks:</p> <pre><code>tasklib:\n  package:\n    name: my_package\n</code></pre> <p>See the user guide for the full config schema.</p>"},{"location":"#features","title":"Features","text":"<p><code>invoke-tasklib</code> provides a comprehensive set of tasks organized into namespaces:</p>"},{"location":"#format-and-lint","title":"\ud83c\udfa8 Format and Lint","text":"<ul> <li><code>format.check-python</code> / <code>format.fix-python</code>: check or format code with ruff</li> <li><code>format.check-docstrings</code> / <code>format.fix-docstrings</code>: check or format docstrings with   docformatter</li> <li><code>format.check-shell</code> / <code>format.fix-shell</code>: check shell scripts with shellcheck, or format them   with shfmt</li> <li><code>lint.check-lint</code>: check linting with ruff</li> </ul> <p>Learn more \u2192</p>"},{"location":"#type-checking","title":"\ud83d\udd2c Type Checking","text":"<ul> <li><code>types.check</code>: check type hints with pyright</li> </ul> <p>Learn more \u2192</p>"},{"location":"#testing","title":"\ud83e\uddea Testing","text":"<ul> <li><code>test.doctest</code>, <code>test.unit</code>, <code>test.integration</code>, <code>test.functional</code>, <code>test.all</code>, <code>test.benchmark</code></li> </ul> <p>Learn more \u2192</p>"},{"location":"#environment-management","title":"\ud83d\udc0d Environment Management","text":"<ul> <li><code>env.create-venv</code>, <code>env.install</code>, <code>env.update</code>, <code>env.show-installed-packages</code>,   <code>env.show-python-config</code></li> </ul> <p>Learn more \u2192</p>"},{"location":"#release","title":"\ud83d\udce6 Release","text":"<ul> <li><code>release.build</code>, <code>release.pypi</code></li> </ul> <p>Learn more \u2192</p>"},{"location":"#documentation","title":"\ud83d\udcda Documentation","text":"<ul> <li><code>doc.publish-dev</code>, <code>doc.publish-latest</code></li> </ul> <p>Learn more \u2192</p>"},{"location":"#contributing","title":"Contributing","text":"<p>Contributions are welcome! Please open an issue first to discuss significant changes. See the developer guide for how to set up a development environment and the project's conventions.</p>"},{"location":"#api-stability","title":"API Stability","text":"<p> Important: As <code>invoke-tasklib</code> is under active development, its API is not yet stable and may change between releases. We recommend pinning a specific version in your project's dependencies to ensure consistent behavior.</p>"},{"location":"#license","title":"License","text":"<p><code>invoke-tasklib</code> is licensed under the BSD 3-Clause \"New\" or \"Revised\" license available in LICENSE.</p>"},{"location":"get_started/","title":"Get Started","text":"<p>We highly recommend installing <code>invoke-tasklib</code> in a virtual environment to avoid dependency conflicts.</p>"},{"location":"get_started/#using-uv-recommended","title":"Using uv (recommended)","text":"<p><code>uv</code> is a fast Python package installer and resolver:</p> <pre><code>uv add --dev invoke-tasklib\n</code></pre>"},{"location":"get_started/#using-pip","title":"Using pip","text":"<p>Alternatively, you can use <code>pip</code>:</p> <pre><code>pip install invoke-tasklib\n</code></pre>"},{"location":"get_started/#setting-up-a-project","title":"Setting up a project","text":"<p>Starting from a typical project layout:</p> <pre><code>my_package/\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 my_package/\n\u2502       \u2514\u2500\u2500 __init__.py\n\u251c\u2500\u2500 tests/\n\u2502   \u2514\u2500\u2500 unit/\n\u251c\u2500\u2500 pyproject.toml\n</code></pre>"},{"location":"get_started/#1-add-a-taskspy","title":"1. Add a <code>tasks.py</code>","text":"<p>Add a <code>tasks.py</code> at the root of your project that imports the shared namespace:</p> <pre><code># tasks.py\nfrom invoke_tasklib import ns\n</code></pre>"},{"location":"get_started/#2-add-an-invokeyaml","title":"2. Add an <code>invoke.yaml</code>","text":"<p>Add an <code>invoke.yaml</code> at the root of your project with, at minimum, the package name:</p> <pre><code>tasklib:\n  package:\n    name: my_package\n</code></pre>"},{"location":"get_started/#3-list-the-available-tasks","title":"3. List the available tasks","text":"<pre><code>invoke --list\n</code></pre> <pre><code>Available tasks:\n\n  doc.publish-dev              Publish development (e.g. unstable) docs.\n  doc.publish-latest           Publish latest (e.g. stable) docs.\n  env.create-venv              Create a virtual environment and install invoke.\n  env.install                  Install project dependencies and the package in editable mode.\n  env.show-installed-packages  Show the installed packages.\n  env.show-python-config       Show the python configuration.\n  env.update                   Update dependencies and pre-commit hooks to their latest versions.\n  format.check-docstrings      Check docstring formatting with docformatter without modifying files.\n  format.check-python          Check code format with ruff without modifying files.\n  format.check-shell           Check shell scripts with shellcheck.\n  format.fix-docstrings        Format docstrings in source code with docformatter.\n  format.fix-python            Format code in place with ruff.\n  format.fix-shell             Format shell scripts in place with shfmt.\n  lint.check-lint               Check code linting with ruff.\n  release.build                 Build the package and verify it can be installed.\n  release.pypi                  Build and publish the package to PyPI.\n  test.all                      Run all tests (unit, integration, and functional).\n  test.benchmark                Run performance benchmarks.\n  test.doctest                  Run doctests on source code.\n  test.functional               Run functional tests.\n  test.integration              Run integration tests.\n  test.unit                     Run unit tests.\n  types.check                   Check type hints with pyright.\n</code></pre>"},{"location":"get_started/#4-run-your-first-tasks","title":"4. Run your first tasks","text":"<pre><code>invoke format.check-python lint.check-lint\ninvoke test.unit\n</code></pre> <p>If a task fails because a tool such as <code>ruff</code> or <code>pytest</code> isn't installed yet, run <code>env.install</code> first:</p> <pre><code>invoke env.install\n</code></pre>"},{"location":"get_started/#next-steps","title":"Next Steps","text":"<ul> <li>User Guide: a full task reference organized by namespace, with an   at-a-glance table of every task.</li> <li>Config: the full <code>invoke.yaml</code> schema and how path defaults are derived.</li> <li>Troubleshooting: fixes for common setup errors (missing package name,   missing tools, <code>PYPI_TOKEN</code>, ...).</li> </ul>"},{"location":"get_started/#installing-from-source","title":"Installing from source","text":"<p>To install <code>invoke-tasklib</code> from source, you can follow the steps below.</p> <p>First, clone the git repository:</p> <pre><code>git clone git@github.com:durandtibo/invoke-tasklib.git\ncd invoke-tasklib\n</code></pre> <p>Note: <code>invoke-tasklib</code> requires Python 3.10 or higher.</p> <p>It is recommended to create a virtual environment (this step is optional):</p> <pre><code>uv venv --clear\nsource .venv/bin/activate\n</code></pre> <p>Then, install the project in editable mode along with its development dependencies:</p> <pre><code>uv sync --group dev\n</code></pre> <p>Finally, you can test the installation by running the unit tests:</p> <pre><code>python -m pytest tests/\n</code></pre>"},{"location":"dev/architecture/","title":"Architecture and Design","text":"<p>This document describes the internal architecture and design principles of <code>invoke-tasklib</code>.</p>"},{"location":"dev/architecture/#overview","title":"Overview","text":"<p><code>invoke-tasklib</code> is designed around a simple idea: task modules are plain Invoke collections that share one config-resolution function. The core design follows these principles:</p> <ol> <li>One config entry point: every task reads project-specific values through    <code>invoke_tasklib.config.get_config</code>, never directly from <code>c.config</code>.</li> <li>Convention over configuration: sensible defaults derived from <code>package.name</code> mean most    projects only set one value (<code>tasklib.package.name</code>).</li> <li>Composability: the pre-built <code>ns</code> namespace is a convenience; any subset of task modules    can be imported and combined with project-specific tasks.</li> <li>No hidden state: tasks only run external commands (<code>ruff</code>, <code>pytest</code>, <code>uv</code>, <code>mike</code>, ...);    they don't maintain state between invocations.</li> </ol>"},{"location":"dev/architecture/#core-components","title":"Core Components","text":""},{"location":"dev/architecture/#1-config-resolution","title":"1. Config Resolution","text":"<p><code>invoke_tasklib.config.get_config(c)</code> is the single source of truth for project-specific values. It:</p> <ul> <li>Reads the <code>tasklib</code> key from the Invoke config (<code>c.config</code>), which Invoke itself populates from   <code>invoke.yaml</code>.</li> <li>Merges it on top of <code>DEFAULT_PACKAGE</code>/<code>DEFAULT_PATHS</code>.</li> <li>Derives unset <code>paths.*</code> entries from <code>package.name</code> (for <code>paths.src</code>) or <code>paths.tests</code> (for the   <code>unit_tests</code>/<code>integration_tests</code>/<code>functional_tests</code>/<code>benchmarks</code> entries).</li> <li>Raises <code>ValueError</code> if <code>package.name</code> is missing, since nearly every task needs it.</li> </ul> <pre><code>class TasklibConfig(TypedDict):\n    package: PackageConfig\n    paths: PathsConfig\n</code></pre> <p>Returning a <code>TypedDict</code> rather than a raw dict gives task authors static typing on <code>cfg[\"package\"][\"name\"]</code> and similar accesses.</p>"},{"location":"dev/architecture/#2-task-modules","title":"2. Task Modules","text":"<p>Each task module (<code>format.py</code>, <code>lint.py</code>, <code>types.py</code>, <code>test.py</code>, <code>env.py</code>, <code>release.py</code>, <code>doc.py</code>) is a flat collection of <code>@task</code>-decorated functions. A module groups tasks that operate on the same underlying tool or concern (e.g. all of <code>test.py</code> wraps <code>pytest</code>).</p> <p>Tasks that need project-specific values call <code>get_config(c)</code> at the top of the function body; tasks that don't (e.g. <code>lint.check_lint</code>) skip it entirely.</p>"},{"location":"dev/architecture/#3-the-ns-namespace","title":"3. The <code>ns</code> Namespace","text":"<p><code>invoke_tasklib/__init__.py</code> builds a single <code>Collection</code> (<code>ns</code>) by adding each task module as a sub-collection, named after the module:</p> <pre><code>ns = Collection()\nns.add_collection(Collection.from_module(format), name=\"format\")\n...\n</code></pre> <p>This is what gives consuming projects <code>format.check-python</code>, <code>test.unit</code>, etc. via a single <code>from invoke_tasklib import ns</code> in their <code>tasks.py</code>.</p>"},{"location":"dev/architecture/#data-flow","title":"Data Flow","text":"<p>Here's how a task invocation flows through the system:</p> <pre><code>User runs `invoke test.unit --cov`\n    \u2193\nInvoke loads invoke.yaml into c.config\n    \u2193\ntest.unit(c, cov=True) is called\n    \u2193\nget_config(c) resolves package.name and paths.unit_tests\n    \u2193\nTask builds the pytest command line\n    \u2193\nc.run(cmd, pty=True) executes it as a subprocess\n</code></pre>"},{"location":"dev/architecture/#design-decisions","title":"Design Decisions","text":""},{"location":"dev/architecture/#why-a-shared-get_config-instead-of-per-task-config","title":"Why a Shared <code>get_config</code> Instead of Per-Task Config?","text":"<p>Rationale: A single resolution function keeps defaults and validation in one place, and guarantees every task sees the same resolved paths (e.g. <code>test.unit</code> and <code>types.check</code> agree on <code>package.name</code>).</p> <p>Trade-off: Every task pays the (cheap) cost of re-resolving config on each call, rather than resolving it once. This is negligible since <code>get_config</code> does no I/O beyond reading <code>c.config</code>.</p>"},{"location":"dev/architecture/#why-check_targetfix_target-naming","title":"Why <code>check_&lt;target&gt;</code>/<code>fix_&lt;target&gt;</code> Naming?","text":"<p>Rationale: Makes the read-only/mutating counterpart of a task discoverable by name alone, and lets CI safely run every <code>check_*</code> task without risk of it modifying the working tree.</p> <p>Trade-off: Slightly more verbose task names than a single <code>format</code> task with a <code>--fix</code> flag, but the split keeps <code>invoke --list</code> self-documenting.</p>"},{"location":"dev/architecture/#why-typeddict-for-config","title":"Why <code>TypedDict</code> for Config?","text":"<p>Rationale: Static type checking (via pyright) catches typos like <code>cfg[\"path\"][\"src\"]</code> at development time, without requiring a runtime schema validation dependency.</p> <p>Trade-off: <code>TypedDict</code> doesn't validate values at runtime beyond what <code>get_config</code> does explicitly (e.g. the <code>package.name</code> check).</p>"},{"location":"dev/architecture/#testing-strategy","title":"Testing Strategy","text":"<p>Tests use <code>invoke.context.MockContext</code>, which records every <code>c.run(...)</code> call without executing it. This lets tests assert on the exact shell command a task would run, without depending on <code>ruff</code>, <code>pytest</code>, <code>uv</code>, etc. being installed or configured in the test environment.</p>"},{"location":"dev/architecture/#future-directions","title":"Future Directions","text":"<p>Potential areas for enhancement:</p> <ol> <li>Additional task modules for other common project needs (e.g. changelog generation).</li> <li>A <code>--config-schema</code> task to validate a project's <code>invoke.yaml</code> against the config schema    ahead of time.</li> </ol>"},{"location":"dev/architecture/#references","title":"References","text":"<ul> <li>Invoke documentation</li> <li><code>uv</code> documentation</li> <li><code>mike</code> documentation</li> </ul>"},{"location":"dev/development/","title":"Development Guide","text":"<p>This guide covers setting up your development environment and common development tasks for <code>invoke-tasklib</code> itself.</p>"},{"location":"dev/development/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or higher</li> <li><code>uv</code> for dependency management</li> <li>Git for version control</li> </ul>"},{"location":"dev/development/#initial-setup","title":"Initial Setup","text":""},{"location":"dev/development/#1-fork-and-clone","title":"1. Fork and Clone","text":"<pre><code>git clone https://github.com/YOUR-USERNAME/invoke-tasklib.git\ncd invoke-tasklib\n</code></pre>"},{"location":"dev/development/#2-set-up-a-virtual-environment","title":"2. Set Up a Virtual Environment","text":"<pre><code>uv venv --clear\nsource .venv/bin/activate\n</code></pre>"},{"location":"dev/development/#3-install-dependencies","title":"3. Install Dependencies","text":"<pre><code>uv sync --group dev\n</code></pre>"},{"location":"dev/development/#4-set-up-pre-commit-hooks","title":"4. Set Up Pre-commit Hooks","text":"<pre><code>pre-commit install\n</code></pre>"},{"location":"dev/development/#development-workflow","title":"Development Workflow","text":""},{"location":"dev/development/#running-tests","title":"Running Tests","text":"<pre><code>python -m pytest tests/\n</code></pre> <p>With coverage:</p> <pre><code>python -m pytest --cov=invoke_tasklib --cov-report term tests/\n</code></pre> <p>Since <code>invoke-tasklib</code> itself uses <code>invoke</code> as its build tool, the project's own <code>tasks.py</code> (which composes tasks from <code>invoke_tasklib</code>) can also be used once the package is installed in editable mode - see the Makefile and <code>invoke --list</code> for the available tasks.</p>"},{"location":"dev/development/#code-quality","title":"Code Quality","text":"<pre><code>ruff format --check .   # format.check-python equivalent\nruff check .             # lint.check-lint equivalent\npyright --verifytypes invoke_tasklib --ignoreexternal\n</code></pre> <p>Or, once the package is installed:</p> <pre><code>invoke format.check-python\ninvoke lint.check-lint\ninvoke types.check\n</code></pre>"},{"location":"dev/development/#documentation","title":"Documentation","text":"<p>Build documentation locally:</p> <pre><code>mkdocs serve -f docs/mkdocs.yml\n</code></pre> <p>Then open http://127.0.0.1:8000 in your browser.</p> <p>Build without serving:</p> <pre><code>mkdocs build -f docs/mkdocs.yml\n</code></pre>"},{"location":"dev/development/#project-structure","title":"Project Structure","text":"<pre><code>invoke-tasklib/\n\u251c\u2500\u2500 .github/                  # GitHub configuration (workflows, etc.)\n\u251c\u2500\u2500 docs/                     # Documentation\n\u2502   \u251c\u2500\u2500 docs/                # Documentation source\n\u2502   \u2514\u2500\u2500 mkdocs.yml            # MkDocs configuration\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 invoke_tasklib/\n\u2502       \u251c\u2500\u2500 __init__.py       # Public API: `ns` and individual task modules\n\u2502       \u251c\u2500\u2500 config.py         # Shared config resolution (`get_config`)\n\u2502       \u251c\u2500\u2500 format.py         # format.* tasks\n\u2502       \u251c\u2500\u2500 lint.py           # lint.* tasks\n\u2502       \u251c\u2500\u2500 types.py          # types.* tasks\n\u2502       \u251c\u2500\u2500 test.py           # test.* tasks\n\u2502       \u251c\u2500\u2500 env.py            # env.* tasks\n\u2502       \u251c\u2500\u2500 release.py        # release.* tasks\n\u2502       \u2514\u2500\u2500 doc.py            # doc.* tasks\n\u251c\u2500\u2500 tests/                    # Test files, one module per task module\n\u251c\u2500\u2500 tasks.py                  # invoke-tasklib's own tasks.py (dogfoods `ns`)\n\u251c\u2500\u2500 invoke.yaml                # invoke-tasklib's own tasklib config\n\u251c\u2500\u2500 pyproject.toml             # Project configuration\n\u2514\u2500\u2500 README.md\n</code></pre>"},{"location":"dev/development/#adding-a-new-task","title":"Adding a New Task","text":"<ol> <li>Pick the right module (<code>format.py</code>, <code>lint.py</code>, <code>types.py</code>, <code>test.py</code>, <code>env.py</code>, <code>release.py</code>,    or <code>doc.py</code>), or create a new one if the task doesn't fit an existing namespace.</li> <li>Follow the <code>check_&lt;target&gt;</code>/<code>fix_&lt;target&gt;</code> naming convention when applicable (see    Format).</li> <li>Call <code>get_config(c)</code> if the task needs project-specific paths or the package name, rather than    hardcoding them.</li> <li>Write a docstring (Google style) describing the task; it is used both by <code>invoke --list --help</code>    and by the reference docs.</li> <li>Add unit tests using <code>invoke.context.MockContext</code> (see existing tests for the pattern).</li> <li>If the module is new, register it in <code>src/invoke_tasklib/__init__.py</code>'s <code>ns</code> and <code>__all__</code>.</li> <li>Update the README task    table and the corresponding user guide page.</li> </ol>"},{"location":"dev/development/#testing-guidelines","title":"Testing Guidelines","text":"<p>Tests use <code>invoke.context.MockContext</code> to capture the shell commands a task would run, without actually running them:</p> <pre><code>from invoke.config import Config\nfrom invoke.context import MockContext\n\nfrom invoke_tasklib import lint\n\n\ndef test_check_lint() -&gt; None:\n    c = MockContext(config=Config(overrides={\"tasklib\": {\"package\": {\"name\": \"mypkg\"}}}), run=True)\n    lint.check_lint(c)\n    assert c.run.call_args.args[0] == \"ruff check --output-format=github .\"\n</code></pre>"},{"location":"dev/development/#continuous-integration","title":"Continuous Integration","text":"<p>CI runs on every push and PR: linting, type checking, and the test suite. Documentation is built on every push and deployed via <code>doc.publish-dev</code>/<code>doc.publish-latest</code> from a release workflow.</p>"},{"location":"refs/config/","title":"Config","text":""},{"location":"refs/config/#invoke_tasklib.config","title":"invoke_tasklib.config","text":"<p>Config resolution for invoke-tasklib tasks.</p> <p>Consuming projects set project-specific values under a <code>tasklib</code> key in their <code>invoke.yaml</code>, e.g.::</p> <pre><code>tasklib:\n  package:\n    name: coola\n  paths:\n    docs_config: docs/mkdocs.yml\n</code></pre> <p>Only <code>package.name</code> is required; everything else has a default derived from it.</p>"},{"location":"refs/config/#invoke_tasklib.config.PackageConfig","title":"invoke_tasklib.config.PackageConfig","text":"<p>               Bases: <code>TypedDict</code></p> <p>Resolved <code>package</code> config section.</p>"},{"location":"refs/config/#invoke_tasklib.config.PathsConfig","title":"invoke_tasklib.config.PathsConfig","text":"<p>               Bases: <code>TypedDict</code></p> <p>Resolved <code>paths</code> config section.</p>"},{"location":"refs/config/#invoke_tasklib.config.TasklibConfig","title":"invoke_tasklib.config.TasklibConfig","text":"<p>               Bases: <code>TypedDict</code></p> <p>Resolved tasklib config.</p>"},{"location":"refs/config/#invoke_tasklib.config.get_config","title":"invoke_tasklib.config.get_config","text":"<pre><code>get_config(c: Context) -&gt; TasklibConfig\n</code></pre> <p>Return the effective tasklib config, merging user overrides from <code>invoke.yaml</code> (under the <code>tasklib</code> key) on top of the defaults.</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <p>Returns:</p> Type Description <code>TasklibConfig</code> <p>A dict with resolved <code>package</code> and <code>paths</code> sections.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>tasklib.package.name</code> is not set.</p>"},{"location":"refs/doc/","title":"Doc","text":""},{"location":"refs/doc/#invoke_tasklib.doc","title":"invoke_tasklib.doc","text":"<p>Documentation publishing tasks (versioned docs via mike).</p>"},{"location":"refs/doc/#invoke_tasklib.doc.publish_dev","title":"invoke_tasklib.doc.publish_dev","text":"<pre><code>publish_dev(c: Context) -&gt; None\n</code></pre> <p>Publish development (e.g. unstable) docs.</p>"},{"location":"refs/doc/#invoke_tasklib.doc.publish_latest","title":"invoke_tasklib.doc.publish_latest","text":"<pre><code>publish_latest(c: Context) -&gt; None\n</code></pre> <p>Publish latest (e.g. stable) docs.</p> <p>Requires the <code>feu</code> and <code>packaging</code> packages to determine the latest version tag.</p>"},{"location":"refs/env/","title":"Env","text":""},{"location":"refs/env/#invoke_tasklib.env","title":"invoke_tasklib.env","text":"<p>Environment and dependency management tasks.</p>"},{"location":"refs/env/#invoke_tasklib.env.create_venv","title":"invoke_tasklib.env.create_venv","text":"<pre><code>create_venv(c: Context) -&gt; None\n</code></pre> <p>Create a virtual environment and install invoke.</p> Note <p>The virtual environment will be created in the .venv directory and any existing environment will be cleared.</p>"},{"location":"refs/env/#invoke_tasklib.env.install","title":"invoke_tasklib.env.install","text":"<pre><code>install(\n    c: Context,\n    optional_deps: bool = True,\n    dev_deps: bool = True,\n    docs_deps: bool = False,\n) -&gt; None\n</code></pre> <p>Install project dependencies and the package in editable mode.</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <code>optional_deps</code> <code>bool</code> <p>If True, install all optional dependencies defined in the project extras. Default is True.</p> <code>True</code> <code>dev_deps</code> <code>bool</code> <p>If True, install development dependencies. Default is True.</p> <code>True</code> <code>docs_deps</code> <code>bool</code> <p>If True, install documentation generation dependencies. Default is False.</p> <code>False</code>"},{"location":"refs/env/#invoke_tasklib.env.show_installed_packages","title":"invoke_tasklib.env.show_installed_packages","text":"<pre><code>show_installed_packages(c: Context) -&gt; None\n</code></pre> <p>Show the installed packages.</p>"},{"location":"refs/env/#invoke_tasklib.env.show_python_config","title":"invoke_tasklib.env.show_python_config","text":"<pre><code>show_python_config(c: Context) -&gt; None\n</code></pre> <p>Show the python configuration.</p>"},{"location":"refs/env/#invoke_tasklib.env.update","title":"invoke_tasklib.env.update","text":"<pre><code>update(c: Context) -&gt; None\n</code></pre> <p>Update dependencies and pre-commit hooks to their latest versions.</p> Warning <p>This may introduce breaking changes. Review the changes and run tests after updating.</p>"},{"location":"refs/format/","title":"Format","text":""},{"location":"refs/format/#invoke_tasklib.format","title":"invoke_tasklib.format","text":"<p>Code and docstring formatting tasks.</p> <p>Naming convention: <code>check_&lt;target&gt;</code> tasks are read-only (they fail without modifying files); <code>fix_&lt;target&gt;</code> tasks modify files in place.</p>"},{"location":"refs/format/#invoke_tasklib.format.check_docstrings","title":"invoke_tasklib.format.check_docstrings","text":"<pre><code>check_docstrings(c: Context) -&gt; None\n</code></pre> <p>Check docstring formatting with docformatter without modifying files.</p>"},{"location":"refs/format/#invoke_tasklib.format.check_python","title":"invoke_tasklib.format.check_python","text":"<pre><code>check_python(c: Context) -&gt; None\n</code></pre> <p>Check code format with ruff without modifying files.</p>"},{"location":"refs/format/#invoke_tasklib.format.check_shell","title":"invoke_tasklib.format.check_shell","text":"<pre><code>check_shell(c: Context) -&gt; None\n</code></pre> <p>Check shell scripts with shellcheck.</p>"},{"location":"refs/format/#invoke_tasklib.format.fix_docstrings","title":"invoke_tasklib.format.fix_docstrings","text":"<pre><code>fix_docstrings(c: Context) -&gt; None\n</code></pre> <p>Format docstrings in source code with docformatter.</p> Note <p>This modifies files in place. Ensure your work is committed before running this task.</p>"},{"location":"refs/format/#invoke_tasklib.format.fix_python","title":"invoke_tasklib.format.fix_python","text":"<pre><code>fix_python(c: Context) -&gt; None\n</code></pre> <p>Format code in place with ruff.</p> Note <p>This modifies files in place. Ensure your work is committed before running this task.</p>"},{"location":"refs/format/#invoke_tasklib.format.fix_shell","title":"invoke_tasklib.format.fix_shell","text":"<pre><code>fix_shell(c: Context) -&gt; None\n</code></pre> <p>Format shell scripts in place with shfmt.</p> Note <p>This modifies files in place. Ensure your work is committed before running this task.</p>"},{"location":"refs/lint/","title":"Lint","text":""},{"location":"refs/lint/#invoke_tasklib.lint","title":"invoke_tasklib.lint","text":"<p>Lint tasks.</p>"},{"location":"refs/lint/#invoke_tasklib.lint.check_lint","title":"invoke_tasklib.lint.check_lint","text":"<pre><code>check_lint(c: Context) -&gt; None\n</code></pre> <p>Check code linting with ruff.</p>"},{"location":"refs/release/","title":"Release","text":""},{"location":"refs/release/#invoke_tasklib.release","title":"invoke_tasklib.release","text":"<p>Build and publish tasks (PyPI package).</p>"},{"location":"refs/release/#invoke_tasklib.release.build","title":"invoke_tasklib.release.build","text":"<pre><code>build(c: Context, check: bool = False) -&gt; None\n</code></pre> <p>Build the package and verify it can be installed.</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <code>check</code> <code>bool</code> <p>If True, also check the package's PyPI metadata with twine. Default is False.</p> <code>False</code>"},{"location":"refs/release/#invoke_tasklib.release.pypi","title":"invoke_tasklib.release.pypi","text":"<pre><code>pypi(c: Context) -&gt; None\n</code></pre> <p>Build and publish the package to PyPI.</p>"},{"location":"refs/test/","title":"Test","text":""},{"location":"refs/test/#invoke_tasklib.test","title":"invoke_tasklib.test","text":"<p>Test and benchmark tasks.</p>"},{"location":"refs/test/#invoke_tasklib.test.all","title":"invoke_tasklib.test.all","text":"<pre><code>all(c: Context, cov: bool = False) -&gt; None\n</code></pre> <p>Run all tests (unit, integration, and functional).</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <code>cov</code> <code>bool</code> <p>If True, generate coverage reports in HTML, XML, and terminal formats. Default is False.</p> <code>False</code>"},{"location":"refs/test/#invoke_tasklib.test.benchmark","title":"invoke_tasklib.test.benchmark","text":"<pre><code>benchmark(c: Context) -&gt; None\n</code></pre> <p>Run performance benchmarks.</p>"},{"location":"refs/test/#invoke_tasklib.test.doctest","title":"invoke_tasklib.test.doctest","text":"<pre><code>doctest(c: Context) -&gt; None\n</code></pre> <p>Run doctests on source code.</p>"},{"location":"refs/test/#invoke_tasklib.test.functional","title":"invoke_tasklib.test.functional","text":"<pre><code>functional(c: Context, cov: bool = False) -&gt; None\n</code></pre> <p>Run functional tests.</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <code>cov</code> <code>bool</code> <p>If True, generate coverage reports (appended). Default is False.</p> <code>False</code>"},{"location":"refs/test/#invoke_tasklib.test.integration","title":"invoke_tasklib.test.integration","text":"<pre><code>integration(c: Context, cov: bool = False) -&gt; None\n</code></pre> <p>Run integration tests.</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <code>cov</code> <code>bool</code> <p>If True, generate coverage reports (appended). Default is False.</p> <code>False</code>"},{"location":"refs/test/#invoke_tasklib.test.unit","title":"invoke_tasklib.test.unit","text":"<pre><code>unit(c: Context, cov: bool = False) -&gt; None\n</code></pre> <p>Run unit tests.</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>Context</code> <p>The invoke context.</p> required <code>cov</code> <code>bool</code> <p>If True, generate coverage reports. Default is False.</p> <code>False</code>"},{"location":"refs/types/","title":"Types","text":""},{"location":"refs/types/#invoke_tasklib.types","title":"invoke_tasklib.types","text":"<p>Type-checking tasks.</p>"},{"location":"refs/types/#invoke_tasklib.types.check","title":"invoke_tasklib.types.check","text":"<pre><code>check(c: Context) -&gt; None\n</code></pre> <p>Check type hints with pyright.</p>"},{"location":"uguide/config/","title":"Config","text":"<p> This page describes the <code>invoke_tasklib.config</code> module, which resolves the shared config that all <code>invoke-tasklib</code> tasks read from.</p> <p>Prerequisites: You'll need to know a bit of Python and Invoke.</p>"},{"location":"uguide/config/#overview","title":"Overview","text":"<p>Every task in <code>invoke-tasklib</code> calls <code>get_config(c)</code> to read project-specific values from an <code>invoke.yaml</code> at the root of the consuming project, under a <code>tasklib</code> key:</p> <pre><code>tasklib:\n  package:\n    name: my_package\n    python_version: \"3.14\"\n  paths:\n    src: src/my_package\n    tests: tests\n    unit_tests: tests/unit\n    integration_tests: tests/integration\n    functional_tests: tests/functional\n    benchmarks: tests/benchmarks\n    docs_config: docs/mkdocs.yml\n</code></pre> <p>Only <code>tasklib.package.name</code> is required. Everything else has a default, and most of the <code>paths</code> defaults are derived from <code>package.name</code> or from <code>paths.tests</code>.</p>"},{"location":"uguide/config/#defaults","title":"Defaults","text":"Key Default <code>package.python_version</code> <code>\"3.14\"</code> <code>paths.src</code> <code>src/&lt;package.name&gt;</code> <code>paths.tests</code> <code>tests</code> <code>paths.unit_tests</code> <code>&lt;paths.tests&gt;/unit</code> <code>paths.integration_tests</code> <code>&lt;paths.tests&gt;/integration</code> <code>paths.functional_tests</code> <code>&lt;paths.tests&gt;/functional</code> <code>paths.benchmarks</code> <code>&lt;paths.tests&gt;/benchmarks</code> <code>paths.docs_config</code> <code>docs/mkdocs.yml</code> <p>If <code>tasklib.package.name</code> is missing, <code>get_config</code> raises a <code>ValueError</code>.</p>"},{"location":"uguide/config/#minimal-config","title":"Minimal Config","text":"<p>The smallest valid <code>invoke.yaml</code> only sets the package name:</p> <pre><code>tasklib:\n  package:\n    name: my_package\n</code></pre> <p>With this, <code>paths.src</code> resolves to <code>src/my_package</code>, <code>paths.unit_tests</code> resolves to <code>tests/unit</code>, and so on.</p>"},{"location":"uguide/config/#overriding-paths","title":"Overriding Paths","text":"<p>Override any individual path without affecting the others' defaults, for example when tests live outside <code>tests/</code>:</p> <pre><code>tasklib:\n  package:\n    name: my_package\n  paths:\n    tests: test\n    benchmarks: test/perf\n</code></pre> <p>Here <code>paths.unit_tests</code> still resolves to <code>&lt;paths.tests&gt;/unit</code>, i.e. <code>test/unit</code>, since it is derived from the overridden <code>paths.tests</code>.</p>"},{"location":"uguide/config/#using-get_config-in-a-custom-task","title":"Using <code>get_config</code> in a Custom Task","text":"<p><code>get_config</code> is the same function every built-in task uses, so a project-specific task can read the same config:</p> <pre><code>from invoke.tasks import task\nfrom invoke_tasklib.config import get_config\n\n\n@task\ndef my_task(c):\n    cfg = get_config(c)\n    print(cfg[\"package\"][\"name\"], cfg[\"paths\"][\"src\"])\n</code></pre>"},{"location":"uguide/config/#see-also","title":"See Also","text":"<ul> <li>Get Started: setting up <code>invoke.yaml</code> in a new project.</li> <li><code>invoke_tasklib.config</code> reference</li> </ul>"},{"location":"uguide/doc/","title":"Doc","text":"<p> This page describes the <code>invoke_tasklib.doc</code> module, which provides tasks to publish versioned documentation with <code>mike</code>, on top of MkDocs.</p>"},{"location":"uguide/doc/#overview","title":"Overview","text":"Task Behavior <code>doc.publish-dev</code> Publishes development (unstable) docs <code>doc.publish-latest</code> Publishes latest (stable) docs <p>Both tasks read <code>paths.docs_config</code> from the resolved config (default: <code>docs/mkdocs.yml</code>) and pass it to <code>mike</code> via <code>--config-file</code>.</p>"},{"location":"uguide/doc/#publishing-development-docs","title":"Publishing Development Docs","text":"<pre><code>invoke doc.publish-dev\n</code></pre> <p>Deletes any previously published <code>main</code> version (if it exists) and deploys the current docs under the <code>main</code> version with the <code>dev</code> alias, then pushes to the configured remote (typically <code>gh-pages</code>).</p>"},{"location":"uguide/doc/#publishing-latest-stable-docs","title":"Publishing Latest (Stable) Docs","text":"<pre><code>invoke doc.publish-latest\n</code></pre> <p>Determines the latest released version from the most recent git version tag (via <code>feu.local_git.get_last_version_tag_name</code>), truncated to <code>&lt;major&gt;.&lt;minor&gt;</code> (e.g. <code>1.2</code>). If no tag is found, it falls back to <code>0.0</code>.</p> <p>It then deletes any previously published version with that tag, deploys the current docs under that tag with the <code>latest</code> alias, and sets <code>latest</code> as the default version shown to visitors.</p> <p>Note</p> <p><code>doc.publish-latest</code> requires the <code>feu</code> and <code>packaging</code> packages. Install documentation dependencies with <code>invoke env.install --docs-deps</code> before running it.</p>"},{"location":"uguide/doc/#local-preview","title":"Local Preview","text":"<p>These tasks are meant for CI (they <code>--push</code> to the remote). To preview docs locally, run MkDocs directly:</p> <pre><code>mkdocs serve -f docs/mkdocs.yml\n</code></pre>"},{"location":"uguide/doc/#see-also","title":"See Also","text":"<ul> <li>Config: where <code>paths.docs_config</code> comes from.</li> <li><code>invoke_tasklib.doc</code> reference</li> <li>Troubleshooting: fixes for missing <code>feu</code>/<code>packaging</code> errors.</li> </ul>"},{"location":"uguide/env/","title":"Env","text":"<p> This page describes the <code>invoke_tasklib.env</code> module, which provides tasks to set up and inspect a project's Python environment using <code>uv</code>.</p>"},{"location":"uguide/env/#overview","title":"Overview","text":"Task Behavior <code>env.create-venv</code> Creates a virtual environment and installs invoke <code>env.install</code> Installs project dependencies and the package (editable) <code>env.update</code> Updates dependencies and pre-commit hooks <code>env.show-installed-packages</code> Shows the installed packages <code>env.show-python-config</code> Shows the Python configuration"},{"location":"uguide/env/#creating-a-virtual-environment","title":"Creating a Virtual Environment","text":"<pre><code>invoke env.create-venv\n</code></pre> <p>Creates (or recreates) a <code>.venv</code> using the <code>package.python_version</code> from the resolved config, and installs <code>invoke</code> into it via <code>uv tool install</code>.</p> <p>Warning</p> <p>This clears any existing <code>.venv</code>.</p>"},{"location":"uguide/env/#installing-dependencies","title":"Installing Dependencies","text":"<pre><code>invoke env.install\n</code></pre> <p>Runs <code>uv sync --frozen</code> and installs the package in editable mode. By default this installs optional (extras) and dev dependencies; documentation dependencies are opt-in:</p> <pre><code>invoke env.install --no-optional-deps --no-dev-deps\ninvoke env.install --docs-deps\n</code></pre>"},{"location":"uguide/env/#updating-dependencies","title":"Updating Dependencies","text":"<pre><code>invoke env.update\n</code></pre> <p>Runs <code>uv sync --upgrade</code>, upgrades all <code>uv</code> tools, updates pre-commit hooks (<code>pre-commit autoupdate</code>), and reinstalls the project with documentation dependencies.</p> <p>Warning</p> <p>Updating dependencies may introduce breaking changes. Review the changes and run the test suite afterward.</p>"},{"location":"uguide/env/#inspecting-the-environment","title":"Inspecting the Environment","text":"<pre><code>invoke env.show-installed-packages\ninvoke env.show-python-config\n</code></pre> <p><code>env.show-python-config</code> prints the installed Python versions known to <code>uv</code>, the interpreter <code>uv</code> would select, and the interpreter currently on <code>PATH</code>.</p>"},{"location":"uguide/env/#see-also","title":"See Also","text":"<ul> <li>Config: where <code>package.python_version</code> comes from.</li> <li><code>invoke_tasklib.env</code> reference</li> <li>Troubleshooting: fixes for common install/update errors.</li> </ul>"},{"location":"uguide/format/","title":"Format","text":"<p> This page describes the <code>invoke_tasklib.format</code> module, which provides tasks for checking and fixing code, docstring, and shell script formatting.</p>"},{"location":"uguide/format/#overview","title":"Overview","text":"<p>Tasks follow a <code>check_&lt;target&gt;</code> / <code>fix_&lt;target&gt;</code> naming convention:</p> <ul> <li><code>check_&lt;target&gt;</code> tasks are read-only: they fail with a non-zero exit code if formatting is   wrong, but never modify files.</li> <li><code>fix_&lt;target&gt;</code> tasks modify files in place to match the expected format.</li> </ul> Task Behavior <code>format.check-python</code> Checks Python formatting with ruff (read-only) <code>format.fix-python</code> Formats Python code with ruff (in place) <code>format.check-docstrings</code> Checks docstring formatting with docformatter (read-only) <code>format.fix-docstrings</code> Formats docstrings with docformatter (in place) <code>format.check-shell</code> Checks shell scripts with shellcheck (read-only) <code>format.fix-shell</code> Formats shell scripts with shfmt (in place)"},{"location":"uguide/format/#checking-format","title":"Checking Format","text":"<pre><code>invoke format.check-python\ninvoke format.check-docstrings\ninvoke format.check-shell\n</code></pre> <p>These are safe to run anywhere, including in CI, since they never modify files.</p>"},{"location":"uguide/format/#fixing-format","title":"Fixing Format","text":"<pre><code>invoke format.fix-python\ninvoke format.fix-docstrings\ninvoke format.fix-shell\n</code></pre> <p>Warning</p> <p><code>fix_*</code> tasks modify files in place. Ensure your work is committed before running them.</p>"},{"location":"uguide/format/#docstring-style","title":"Docstring Style","text":"<p><code>format.check-docstrings</code> and <code>format.fix-docstrings</code> run <code>docformatter</code> against <code>paths.src</code> from the resolved config, using the <code>[tool.docformatter]</code> section of the consuming project's <code>pyproject.toml</code> (via <code>--config ./pyproject.toml</code>).</p>"},{"location":"uguide/format/#shell-scripts","title":"Shell Scripts","text":"<p><code>format.check-shell</code> and <code>format.fix-shell</code> operate on every <code>*.sh</code> file in the project, excluding <code>.git/</code>.</p> <p>Note</p> <p><code>shellcheck</code> and <code>shfmt</code> are not Python packages, so they aren't installed by <code>env.install</code>. Install them with your system package manager, e.g. <code>brew install shellcheck shfmt</code> on macOS or <code>apt install shellcheck</code> on Debian/Ubuntu (<code>shfmt</code> there comes from the <code>golang-go</code> toolchain or a release binary).</p>"},{"location":"uguide/format/#see-also","title":"See Also","text":"<ul> <li>Config: where <code>paths.src</code> (used by the docstring tasks) comes from.</li> <li>Lint: the companion <code>lint.check-lint</code> task.</li> <li><code>invoke_tasklib.format</code> reference</li> <li>Troubleshooting: fixes for \"command not found\" errors.</li> </ul>"},{"location":"uguide/lint/","title":"Lint","text":"<p> This page describes the <code>invoke_tasklib.lint</code> module.</p>"},{"location":"uguide/lint/#overview","title":"Overview","text":"Task Behavior <code>lint.check-lint</code> Checks code linting with ruff (read-only)"},{"location":"uguide/lint/#usage","title":"Usage","text":"<pre><code>invoke lint.check-lint\n</code></pre> <p>This runs <code>ruff check --output-format=github .</code>, which is well-suited for use in a GitHub Actions workflow since ruff annotates violations directly on the diff.</p>"},{"location":"uguide/lint/#see-also","title":"See Also","text":"<ul> <li>Format: <code>format.check-python</code>/<code>format.fix-python</code> for formatting (as opposed to   linting) with ruff.</li> <li><code>invoke_tasklib.lint</code> reference</li> <li>Troubleshooting: fixes for \"command not found\" errors.</li> </ul>"},{"location":"uguide/release/","title":"Release","text":"<p> This page describes the <code>invoke_tasklib.release</code> module, which provides tasks to build and publish the package to PyPI using <code>uv</code>.</p>"},{"location":"uguide/release/#overview","title":"Overview","text":"Task Behavior <code>release.build</code> Builds the package and verifies installation (<code>--check</code> also validates metadata with twine) <code>release.pypi</code> Builds and publishes the package to PyPI"},{"location":"uguide/release/#building","title":"Building","text":"<pre><code>invoke release.build\n</code></pre> <p>Runs <code>uv build</code>, then verifies the built wheel can actually be installed and imported by running it in an isolated <code>uv run</code> environment.</p> <p>Pass <code>--check</code> to additionally validate the package's PyPI metadata with <code>twine check</code>:</p> <pre><code>invoke release.build --check\n</code></pre>"},{"location":"uguide/release/#publishing-to-pypi","title":"Publishing to PyPI","text":"<pre><code>invoke release.pypi\n</code></pre> <p>Builds the package (via <code>release.build</code>) and then runs <code>uv publish</code>, using the <code>PYPI_TOKEN</code> environment variable for authentication.</p> <p>Warning</p> <p>This publishes a new version to PyPI, which cannot be undone. Make sure <code>PYPI_TOKEN</code> is set and that you are releasing the intended version before running this task.</p>"},{"location":"uguide/release/#see-also","title":"See Also","text":"<ul> <li><code>invoke_tasklib.release</code> reference</li> <li>Troubleshooting: fixes for <code>PYPI_TOKEN</code> authentication errors.</li> </ul>"},{"location":"uguide/test/","title":"Test","text":"<p> This page describes the <code>invoke_tasklib.test</code> module, which provides tasks to run doctests, unit/integration/functional tests, and benchmarks with <code>pytest</code>.</p>"},{"location":"uguide/test/#overview","title":"Overview","text":"Task Behavior <code>test.doctest</code> Runs doctests on source code <code>test.unit</code> Runs unit tests <code>test.integration</code> Runs integration tests <code>test.functional</code> Runs functional tests <code>test.all</code> Runs all tests (unit, integration, and functional) <code>test.benchmark</code> Runs performance benchmarks <p>All test tasks (except <code>test.benchmark</code>) run <code>pytest</code> with <code>--xdoctest</code> enabled, so doctests embedded in the code under test are also collected.</p>"},{"location":"uguide/test/#running-doctests","title":"Running Doctests","text":"<pre><code>invoke test.doctest\n</code></pre> <p>Runs doctests against <code>paths.src</code> from the resolved config.</p>"},{"location":"uguide/test/#running-tests-by-scope","title":"Running Tests by Scope","text":"<pre><code>invoke test.unit\ninvoke test.integration\ninvoke test.functional\n</code></pre> <p>Each targets its own path (<code>paths.unit_tests</code>, <code>paths.integration_tests</code>, <code>paths.functional_tests</code>), with a longer timeout (60s vs 10s) for integration and functional tests, since they typically exercise more of the system.</p> <pre><code>invoke test.all\n</code></pre> <p>Runs everything in <code>paths.tests</code> in a single <code>pytest</code> invocation, with a 10s timeout.</p>"},{"location":"uguide/test/#coverage","title":"Coverage","text":"<p>Pass <code>--cov</code> to <code>test.unit</code>, <code>test.integration</code>, <code>test.functional</code>, or <code>test.all</code> to generate coverage reports in HTML, XML, and terminal formats:</p> <pre><code>invoke test.unit --cov\n</code></pre> <p><code>test.integration --cov</code> and <code>test.functional --cov</code> append to any existing coverage data (<code>--cov-append</code>), so they compose with a prior <code>test.unit --cov</code> run to build up combined coverage across scopes:</p> <pre><code>invoke test.unit --cov\ninvoke test.integration --cov\ninvoke test.functional --cov\n</code></pre>"},{"location":"uguide/test/#benchmarks","title":"Benchmarks","text":"<pre><code>invoke test.benchmark\n</code></pre> <p>Runs <code>pytest --benchmark-only</code> against <code>paths.benchmarks</code>. This requires <code>pytest-benchmark</code> to be installed.</p>"},{"location":"uguide/test/#see-also","title":"See Also","text":"<ul> <li>Config: the <code>paths.*_tests</code> and <code>paths.benchmarks</code> config used by these tasks.</li> <li><code>invoke_tasklib.test</code> reference</li> <li>Troubleshooting: fixes for \"command not found\" errors.</li> </ul>"},{"location":"uguide/types/","title":"Types","text":"<p> This page describes the <code>invoke_tasklib.types</code> module.</p>"},{"location":"uguide/types/#overview","title":"Overview","text":"Task Behavior <code>types.check</code> Checks type hints with pyright (read-only)"},{"location":"uguide/types/#usage","title":"Usage","text":"<pre><code>invoke types.check\n</code></pre> <p>This runs <code>pyright --verifytypes &lt;package.name&gt; --ignoreexternal</code>, using <code>package.name</code> from the resolved config. <code>--ignoreexternal</code> excludes type completeness issues that originate from third-party dependencies rather than from the project's own code.</p>"},{"location":"uguide/types/#see-also","title":"See Also","text":"<ul> <li>Config: where <code>package.name</code> comes from.</li> <li><code>invoke_tasklib.types</code> reference</li> <li>Troubleshooting: fixes for \"command not found\" errors.</li> </ul>"}]}